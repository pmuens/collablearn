.row
  .span5.offset3
    .page-header
      %h1
        Passwort ändern
    = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put, class: 'form-horizontal' } ) do |f|
      = devise_error_messages!
      = f.hidden_field :reset_password_token
      .control-group
        %label.control-label{for: 'new_password'}
          Neues Passwort
        .controls
          = f.password_field :password, id: 'new_password'
      .control-group
        %label.control-label{for: 'password_confirmation'}
          Passwort wiederholen
        .controls
          = f.password_field :password_confirmation, id: 'password_confirmation'
      .control-group
        .controls
          = f.submit 'Passwort ändern', disable_with: 'Passwort ändern', class: 'btn btn-primary'