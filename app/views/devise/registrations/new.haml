.row  
  .span5.offset3
    .page-header
      %h1
        Registrieren
    = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'form-horizontal' } ) do |f|
      = render 'shared/error_messages', object: resource
      .control-group
        %label.control-label{for: 'email'}
          E-Mail
        .controls
          = f.email_field :email, id: 'email'
      .control-group
        %label.control-label{for: 'username'}
          Benutzername
        .controls
          = f.text_field :username, id: 'username'
      .control-group
        %label.control-label{for: 'password'}
          Passwort
        .controls
          = f.password_field :password, id: 'password'
      .control-group
        %label.control-label{for: 'password_confirmation'}
          Passwort wiederholen
        .controls
          = f.password_field :password_confirmation, id: 'password_confirmation'
      .control-group
        .controls
          %label.checkbox
            = f.check_box 'terms_of_service'
            Ich akzeptiere die
            = link_to 'Nutzungsbedingungen', terms_of_service_path
      .control-group
        .controls
          = f.submit 'Registrieren', disable_with: 'Registrieren', class: 'btn btn-primary'
    %p
      Du hast schon einen Account? Dann kannst du dich hier 
      = link_to 'einloggen.', new_user_session_path