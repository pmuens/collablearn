.row
  .span12
    %ul.breadcrumb
      %li
        = link_to 'Home', user_path(current_user)
        %span.divider
          #{'/'}
      %li.active
        Neue Frage erstellen
  .span5.offset3
    .page-header
      %h1
        Neue Frage erstellen
    = form_for(@question, html: { class: 'form-horizontal' } ) do |f|
      = render 'shared/error_messages', object: @question
      .control-group
        %label.control-label{for: 'title'}
          Titel
        .controls
          = f.text_field :title, id: 'title'
      .control-group
        %label.control-label{for: 'question'}
          Frage
        .controls
          = f.text_area :question, id: 'question', rows: '6'
      .control-group
        %label.control-label{for: 'answer'}
          Antwort
        .controls
          = f.text_area :answer, id: 'answer', rows: '6'
      .control-group
        %label.control-label{for: 'tag_list'}
          Tags (mit , trennen)
        .controls
          = f.text_field :tag_list, id: 'tag_list'
      .control-group
        .controls
          = f.submit 'Frage erstellen', disable_with: 'Frage erstellen', class: 'btn btn-primary'