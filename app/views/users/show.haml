.row
  .span12
    .hero-unit
      %h1
        - if user_signed_in?
          = @user.username + '\'s Seite'
        - else
          = @user.username
          ist auf Collablearn!
      %p
        = link_to @user.username + ' folgen', '#', class: 'btn btn-primary'
  .span12
    .page-header
      %h1
        = @user.username + '\'s Lernlisten'